# Hexo Configuration
## Docs: https://hexo.io/docs/configuration.html
## Source: https://github.com/hexojs/hexo/

# Site
title: Drifter Observatory
subtitle: '算法、CTF 与深空工程日志'
description: 'Drifter2005 的个人技术博客，记录算法训练、CTF 复盘、密码学与安全工程思考。'
keywords: algorithm, CTF, cybersecurity, cryptography, reverse engineering, pwn, data structures
author: Drifter2005
language: zh-CN
timezone: Asia/Shanghai

# URL
## Set your site url here. For example, if you use GitHub Page, set url as 'https://username.github.io/project'
url: https://drifter2005.github.io
permalink: :year/:month/:day/:title/
permalink_defaults:
pretty_urls:
  trailing_index: true # Set to false to remove trailing 'index.html' from permalinks
  trailing_html: true # Set to false to remove trailing '.html' from permalinks

# Directory
source_dir: source
public_dir: public
tag_dir: tags
archive_dir: archives
category_dir: categories
code_dir: downloads/code
i18n_dir: :lang
skip_render:

# Writing
new_post_name: :title.md # File name of new posts
default_layout: post
titlecase: false # Transform title into titlecase
external_link:
  enable: true # Open external links in new tab
  field: site # Apply to the whole site
  exclude: ''
filename_case: 0
render_drafts: false
post_asset_folder: false
relative_link: false
future: true
syntax_highlighter: highlight.js
highlight:
  line_number: true
  auto_detect: false
  tab_replace: ''
  wrap: true
  hljs: false
prismjs:
  preprocess: true
  line_number: true
  tab_replace: ''

# Home page setting
# path: Root path for your blogs index page. (default = '')
# per_page: Posts displayed per page. (0 = disable pagination)
# order_by: Posts order. (Order by date descending by default)
index_generator:
  path: ''
  per_page: 10
  order_by: -date

# Category & Tag
default_category: notes
category_map:
tag_map:

# Metadata elements
## https://developer.mozilla.org/en-US/docs/Web/HTML/Element/meta
meta_generator: true

# Date / Time format
## Hexo uses Moment.js to parse and display date
## You can customize the date format as defined in
## http://momentjs.com/docs/#/displaying/format/
date_format: YYYY-MM-DD
time_format: HH:mm:ss
## updated_option supports 'mtime', 'date', 'empty'
updated_option: 'mtime'

# Pagination
## Set per_page to 0 to disable pagination
per_page: 10
pagination_dir: page

# Include / Exclude file(s)
## include:/exclude: options only apply to the 'source/' folder
include:
exclude:
ignore:

# Extensions
## Plugins: https://hexo.io/plugins/
## Themes: https://hexo.io/themes/
theme: cosmic

# Deployment
## Docs: https://hexo.io/docs/one-command-deployment
#
# ⚠️ 正常发布流程不需要用这里 —— 推送本仓库 main 后由
#    .github/workflows/deploy.yml 自动构建并发布，那才是唯一权威路径。
#
# ⚠️ repo 必须始终是 Drifter2005.github.io（Pages 仓库）。
#    绝对不要改成本仓库 myblog：hexo-deployer-git 是强制推送构建产物的，
#    指向源码仓库会把 source/ 和 themes/ 整个覆盖掉。（2026-07-26 曾因此丢过一次源码）
deploy:
  type: git
  repo: https://github.com/Drifter2005/Drifter2005.github.io.git
  branch: main
